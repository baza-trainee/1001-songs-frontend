import { PreviewFromUrlPipe } from './preview-from-url.pipe';

describe('PreviewFromUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new PreviewFromUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
