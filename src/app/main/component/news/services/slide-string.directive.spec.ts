import { SlideStringDirective } from './slide-string.directive';

describe('SlideStringDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideStringDirective();
    expect(directive).toBeTruthy();
  });
});
