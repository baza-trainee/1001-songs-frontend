# name: ci-cd

# on:
#   pull_request:
#     branches: [master, dev]

#   push:
#     branches: [master, dev]

# permissions:
#   contents: write

# jobs:
#   build-and-deploy:
#     # concurrency: ci-${{ github.ref }}
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v3
      
#       - name: Set up Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: 18.16.0
#           cache: 'npm'

#       - name: Install dependencies
#         run: |
#           npm ci
#           npm install
#           npm install -g @angular/cli

#       # - name: Build
#       #   run: npm run build -- --prod

#       # - name: TS Lint
#       #   run: npm run lint

#       # - name: SCSS Linter
#       #   run: npm run stylelint

#       # - name: Unit Tests
#       #   run: npx ng test --watch=false --progress=false --browsers=ChromeHeadless

#       - name: Deploy 🚀
#         env:
#           ITHUB_TOKEN: ${{ secrets.DEPLOY_PASSWORD }}
#         run: |

#           ng deploy --repo=https://github.com/baza-trainee/1001-songs-frontend.git --name="Baza Trainee Ukraine" --email=baza.trainee@gmail.com  --no-silent

name: Deploy to GitHub Pages via angular-cli-ghpages

on: [push]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Use Node.js 18.x
      uses: actions/setup-node@v3
      with:
        node-version: 18.x
        cache: 'npm'

    # - name: Install dependencies
    #   run: |
    #     npm ci
    #     npm install
    #     npm install -g @angular/cli

    - name: Deploy 🚀
      # env:
      #   ITHUB_TOKEN: ${{ secrets.TOKEN }}
      run: |
        npm ci
        npm install
        npm install -g @angular/cli
        ng deploy --no-silent